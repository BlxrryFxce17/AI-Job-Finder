require('dotenv').config(); const { findHROnLinkedIn } = require('./utils/scraper.js'); const { callAIWithRetry } = require('./utils/ai.js'); const { discoverEmailForJob } = require('./utils/email.js'); async function run() { console.log('Testing HR...'); const hr = await findHROnLinkedIn('Jobs Ai'); console.log(hr); const email = await discoverEmailForJob('Jobs Ai', 'jobsai.com', '', [], callAIWithRetry, hr?.name); console.log(email); } run();
